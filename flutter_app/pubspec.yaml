name: material_tracker
description: Scan & tracking material incoming to production
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  connectivity_plus: ^5.0.1
  mobile_scanner: ^5.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true